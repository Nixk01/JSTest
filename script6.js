// Temp. str.

const t = 'Best bks of 24';
const au = 'NJ';
const lks = 333;

// concat way

// let res = 'Nixkn is here ' + lks + ' likes'; // lengthy way

// temp. strn
let res = `The books by ${au} has ${lks}`;

let res2 = `
<h2> ${t} </h2>
<p> By ${au} </p>
<span> This blog has ${lks} likes </span>
` // current O/P to consonle later on in tab;

console.log(res2);
